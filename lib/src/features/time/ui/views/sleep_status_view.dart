import 'package:flutter/material.dart';

class SleepStatusView extends StatelessWidget {
  const SleepStatusView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Sleep Status"),
    );
  }
}
